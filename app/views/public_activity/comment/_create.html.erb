Commented:
<% if (comment = activity.trackable) && (activity.trackable.commentable.present?) %>
  <%= strip_tags comment.content %> on
  <%= link_to comment.commentable.title, commentable_url(comment) %>
<% else %>
  which has since been removed
<% end %>