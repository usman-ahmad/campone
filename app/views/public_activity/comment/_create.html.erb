Commented:
<% if (comment = activity.trackable) && (activity.trackable.commentable.present?) %>
  <%= truncate(strip_tags(comment.content), :length => 50) %>
<% else %>
  which has since been removed
<% end %>
