added Task:

<% if activity.trackable %>
   <%= link_to activity.trackable.title, project_task_path(activity.trackable.project, activity.trackable) %>
<% else %>
  which has since been removed
<% end %>