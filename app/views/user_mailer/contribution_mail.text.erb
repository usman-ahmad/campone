<%= t('devise.mailer.invitation_instructions.hello', name_or_email: (@contribution.user.name || @contribution.user.email)) %>

<%= t('devise.mailer.invitation_instructions.someone_added_you', inviter_name: @inviter_name, project_title: @project_title) %>

<%#= link_to t('devise.mailer.invitation_instructions.accept'), join_contribution_url(@contribution.token) %>
<%= link_to url_for(@contribution.project), @contribution.project %>
