<%= render layout: 'common_form', locals: { project: @project, integration: @integration } do |f| %>
  <%= link_to "Authenticate your Asana account", user_asana_omniauth_authorize_path(params: {project_id: @project.friendly_id}), class: 'btn btn-primary', data: {turbolinks: "false"} %>
<% end %>
