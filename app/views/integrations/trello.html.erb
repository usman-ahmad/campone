<%= render layout: 'common_form', locals: { project: @project, integration: @integration } do |f| %>
  <%= link_to "Authenticate your Trello account", user_trello_omniauth_authorize_path(params: {project_id: @project.friendly_id}), class: 'btn btn-primary', data: {turbolinks: "false"} %>
<% end %>
