<% notifications.each do |notification| %>
  <li>
    <span class=""><b><%= notification.activity.owner.name if notification.activity.owner %></b></span>
    <%= render_activity notification.activity %>
  </li>
<% end %>